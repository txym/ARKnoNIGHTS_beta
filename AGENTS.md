# Unity 项目 Codex 工作规范

本文件适用于仓库内的所有 Codex 任务。请从包含 `Assets/`、`Packages/`、`ProjectSettings/` 的 Unity 项目根目录开始工作。

## 1. 事实来源与冲突处理

1. 开始任务前，按需阅读：
   - 当前用户指令；
   - `docs/SPEC.md`：预期行为与验收标准；
   - `docs/TEST_PLAN.md`：验证方法；
   - `docs/ARCHITECTURE.md`：现有架构与边界；
   - `docs/decisions/`：已确认的长期技术决策。
2. 不猜测关键需求。只有当歧义会影响外部行为、数据格式、资源结构、兼容性或验收结果时，才暂停并询问。
3. 对不影响验收、可逆且低风险的细节，采用最小合理假设继续工作，并在最终汇报中明确记录。
4. 当前指令、SPEC、测试和现有实现发生冲突时，不得静默选择其中之一；应说明冲突、影响和推荐处理方式。

## 2. 开始修改前

1. 检查 `git status --short`，识别用户已有的未提交修改；不得覆盖、回滚或格式化无关修改。
2. 阅读待改文件的相关区域，并检查：
   - 被修改符号的直接调用方和引用；
   - 相关接口、基类、事件、序列化字段；
   - 对应测试和项目文档。
3. 复杂、跨模块或长时间任务应先形成计划，至少写清：目标、范围、风险、实施步骤、验证方式和停止条件。除非用户要求持久化，计划可保留在任务线程中；需要跨会话延续时写入 `docs/PLAN.md`。
4. 优先复现问题或建立基线测试，再修改实现。无法复现时不得声称已经修复。

## 3. 修改原则

1. 采用满足验收标准的最小完整修改，不做无关重构、顺手优化或大范围格式化。
2. 保持现有公开接口、命名风格和模块边界。只有在现有设计无法清晰表达新需求时，才新增状态、接口或抽象，并说明理由与迁移影响。
3. 大规模重构必须拆分为可验证的小步骤。使用 Git 分支、提交或 worktree 保留可恢复点；不要在 `Assets/` 内创建“备份副本”。
4. 不得通过硬编码、吞异常、跳过分支、恒真兜底或降低断言来制造“测试通过”。
5. 不得为了通过测试而擅自修改测试期望。只有当需求已变更，或测试本身可被证明确有错误时，才修改测试，并在汇报中说明证据。
6. 行为、公共接口、配置方式或使用方法发生变化时，同步更新相关文档；纯内部实现调整不强制改文档。

## 4. Unity 专项约束

1. 未经明确批准，不升级 Unity Editor、渲染管线、Input System 或其他 Package 版本。
2. 不手动修改或提交生成目录，包括：
   - `Library/`
   - `Temp/`
   - `Logs/`
   - `obj/`
   - `Build/`、`Builds/`
   - `Artifacts/`
3. 保护 Unity 资源 GUID：
   - 不得无意删除、重新生成或错配 `.meta` 文件；
   - 必须移动或重命名资源时，同时处理对应 `.meta`，并验证引用未丢失。
4. 场景、Prefab、ScriptableObject、Animator Controller 等序列化资源只在任务确实需要时修改。修改后必须检查序列化差异、资源引用和运行时行为。
5. 重命名序列化字段时，优先保持兼容或使用适当的迁移机制；不得静默破坏已有场景和 Prefab 数据。
6. 同一项目路径不得同时由多个 Unity Editor 或 batchmode 进程打开。并行任务需要独立 worktree，且 Unity 测试/构建走单一执行队列。
7. 多智能体不得同时修改同一场景、Prefab 或其他高冲突序列化资源。

## 5. 工具、权限与网络

1. 优先使用仓库已有脚本、Unity 官方工具和已配置的 Skill/MCP。
2. 未经明确批准，不得：
   - 安装或升级软件、Unity Package、插件、Skill、MCP；
   - 从互联网下载并执行代码或二进制文件；
   - 修改系统设置、代理、注册表或项目外文件；
   - 推送远程仓库、发布构建或执行其他外部副作用操作。
3. 必须新增外部依赖时，先说明用途、来源、版本、许可证、权限、替代方案和回滚方式，等待批准。
4. 查阅外部技术资料时优先使用 Unity、包维护者或工具官方文档；不得把未经验证的网页方案直接作为项目事实。
5. 不在日志、文档、提交或回复中泄露密钥、令牌、账号、私有地址或其他敏感信息。

## 6. 测试与验证

测试目标是验证 `docs/SPEC.md` 中的意图和可观察行为，而不只是“能编译”或“能启动”。

按任务相关性依次执行：

1. 检查最终 diff，确认没有无关修改和意外资源变化。
2. 检查 Unity 编译结果。
3. 运行相关 EditMode 测试。
4. 运行相关 PlayMode 测试。
5. 运行目标平台构建。
6. 对受影响的真实流程执行冒烟测试，检查结构化结果、日志，必要时再结合截图或 GUI 操作。
7. 对最终修改进行独立审查，重点检查回归、生命周期、事件订阅、序列化、异步流程和测试缺口。

验证规则：

- 测试跳过、工具不可用、许可证失败、项目被占用、超时或日志不完整，都必须标记为“未验证”，不得记为通过。
- 记录实际执行的命令、退出状态、测试数量、失败项和关键日志位置。
- 仅检查截图不能证明内部状态正确；优先使用断言、测试结果、日志和结构化运行数据。
- 失败后先定位根因，再做有实质差异的修复。连续三次不同尝试仍无法推进时，停止并汇报证据、已尝试方案和下一步建议。

## 7. 长任务与多智能体

1. 主智能体负责统一计划、需求解释、冲突处理和最终集成。
2. 优先把只读、可独立的工作委派给子智能体，例如代码探索、测试缺口分析、日志分析和代码审查。
3. 并行写入任务必须使用隔离的分支或 worktree，并在合并前统一审查和回归测试。
4. 在长任务的主要里程碑进行状态同步：已完成、验证结果、当前风险、剩余步骤。除非触发审批或停止条件，不因普通进度更新而等待用户回复。
5. 不进行无意义的持续轮询或无限重试；等待外部进程时使用有上限的超时、重试次数和间隔。

## 8. 完成标准

只有满足当前任务相关的以下条件，才可以声明完成：

- SPEC 中相关验收标准已逐项核对；
- Unity 编译无新增错误；
- 相关测试通过，或未执行项被明确标记并解释；
- 需要时目标平台构建成功、核心流程完成冒烟验证；
- 日志中没有与本次修改相关的新错误或未处理异常；
- 最终 diff 已审查，无无关修改、意外资源变化或用户改动丢失；
- 必要文档已更新；
- 剩余风险和必须人工确认的项目已列出。

最终汇报必须包含：

1. 完成了什么；
2. 修改了哪些文件；
3. 执行了哪些测试或构建，以及结果；
4. 哪些内容尚未验证；
5. 剩余风险与建议的人工检查。

## 9. 必须停止并询问的情况

遇到以下任一情况，不得自行扩大范围或静默降级：

- 需求或验收标准存在实质冲突；
- 关键预期行为无法从当前指令或项目文档确定；
- 需要破坏性迁移、删除数据、批量重写资源或覆盖用户修改；
- 需要升级 Unity/Package、安装新依赖或获取额外权限；
- 需要访问项目外路径、启用网络、发送数据或执行外部副作用操作；
- 测试环境、许可证、硬件或第三方服务阻止可靠验证；
- 连续三次有实质差异的尝试仍无法解决问题。

## 10.游戏机制与需求不确定性

1. 开始任务前必须阅读 `docs/SPEC.md`。
2. `docs/SPEC.md` 是当前已经确认的游戏机制和玩家可见行为的主要依据。
3. 不得因为本项目参考《明日方舟》，就自行照搬原作中未在 SPEC 明确确认的规则。
4. 当以下内容不明确时，必须暂停相关实现并向用户确认：
   - 玩家可见行为；
   - 数值、费用和结算规则；
   - 回合状态及其转换条件；
   - 单位部署、阻挡、攻击和目标选择规则；
   - 多人同步、胜负判定及异常情况处理；
   - SPEC、现有代码、测试或外部资料之间的冲突。
5. 不得通过猜测、临时兜底或降低验收标准来绕过机制缺失。
6. 对不影响游戏规则和玩家可见行为的低风险实现细节，可以采用最小、可逆且符合现有代码风格的方案，无需频繁询问，但必须在最终汇报中说明重要假设。
7. 用户确认新机制后，应同步更新 `docs/SPEC.md`；涉及重要架构取舍时，还应记录到 `docs/decisions/`。