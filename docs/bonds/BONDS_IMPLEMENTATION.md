# BONDS 单位能力实现台账

本文记录 `docs/bonds/BONDS_SPEC.md` 中单位战斗机制的实际实现状态。只有生产数据流和必要验证都完成后才标记“已实现”；只有 Core 能力但尚未接入真实单位源时标记“框架完成”。

## 工作区与主分支同步

- 独立工作树：`G:\ARKnoNIGHTS_beta\.worktrees\bonds-unit-abilities`
- 分支：`codex/bonds-unit-abilities`
- 起点：`22f6e9f`
- 已同步主分支 `txym` 至 `a33f302`；最近一次合并提交为 `741d91f`。
- 当前任务配置了 7 次每小时主分支提交检查；只同步已提交内容。

## 全局技能动画规则

状态：已实现。

- 所有 Skill 动画按 `2×` 播放。
- v2 单位源保存动画 key、真实 Spine 名称和源时长；能力源保存技能使用的动画 key。
- `SkillAnimationCatalogGenerator` 将两份源交叉校验，生成独立的
  `skill-animation-catalog-v1`，不改变冻结的
  `unit-catalog-v1` 和 `ability-catalog-v1`。
- 源时长先按 `ceil(durationSeconds × 20)` 转为源 Tick，再按
  `(sourceTicks + 1) / 2` 计算 `2×` 后有效 Tick；奇数 Tick 向上取整，确保最后一个关键帧播放。
- Skill 事件把动画 key、源 Tick、有效 Tick传给 Track/Event Playback；Spine 使用固定 `2.0` 倍率。
- 已经开始的 Attack 优先完成。技能满 SP 后保持排队，在攻击动画结束后的下一 Tick 施放；Skill 占用期间不能开始下一次攻击或移动。
- 动作优先级：`Death > Attack > Skill > Move > Idle`。

验证：

- `BondsSkillAnimationEditModeTests`：`3/3` 通过，覆盖 `30 → 15`、
  `31 → 16` 和 Tick 100 攻击开始后延迟至 Tick 121 施法。
- `BattleCoreEditModeTests`：合并后 `57/57` 通过。
- `UnitSourceConsumerEditModeTests`：合并后 `6/6` 通过，覆盖独立技能动画目录生成以及冻结单位目录未被改写。

## 特殊索敌、阻挡与行动

状态：Core 框架完成，生产数据接线暂缓。

| 机制 | Core 状态 | 记录 |
|---|---|---|
| 不攻击单位不索敌 | 已完成 | `AttackMethod.None` 不获取目标、不产生 Attack。 |
| 不可成为攻击目标 | 已完成 | `Untargetable` 被动特征会从敌方索敌候选中排除。 |
| 不可阻挡单位 | 已完成 | `blockCapacity=0` 不建立双向阻挡。 |
| 无攻击单位直冲敌方门 | 已完成 | 可移动的非攻击单位直接向对方门移动，不伪造冲家胜负。 |
| 静止单位 | 已完成 | `actionMethod=4` 不移动。 |
| 铁砧定向移动 | 待实现 | `1146` 从己方门移动至部署位置的路径规则尚未进入当前 Core。 |

`BondsTargetingEditModeTests` 已在同步前运行，`5/5` 通过，覆盖不可索敌、无攻击、零阻挡、直冲敌方门和静止行动。

主分支最新 SPEC 明确规定首批迁移期间两个旧 v1 目录字节冻结，而且旧单位目录无法合法表达“不攻击且不阻挡”的组合。因此：

- 保留 Core 表达与测试；
- `UnitCatalogGenerator` 对这种 v2 源显式报
  `UNIT_CATALOG_V1_SOURCE_UNREPRESENTABLE`；
- 不再向冻结目录或当前仅有三个单位的 v2 源伪造
  `1017/1042/1146/1355` 等单位数据；
- 等主分支完成下一代运行时目录迁移后再接入真实特殊单位。

## 已接入的单位能力

### `5503` 果冻小子：定时召唤三个 `5504`

状态：已实现。

- 初始 SP 5、需求 15、自动回复 2 SP/s。
- 没有攻击占用时名义施放 Tick 为 100、250；攻击中达到满 SP 时等待攻击动画完整结束，实际施放后才消费 SP，后续周期随实际施放时点顺延。
- 每次 Skill 事件之后在施法者所在 100cm × 100cm 方形内确定性生成三个 `5504`。
- `skill` 动画源时长 1.5 秒，即 30 Tick；`2×` 后占用 15 Tick。
- 实际 Spine 名称 `Skill` 由独立技能动画目录绑定。

## 已完成的通用能力框架

状态：Core 框架完成，等待真实单位目录接线。

### 被动战斗属性

- 阻挡数加算：覆盖 `1058 +2`、`1081 +2`、`1240 +1`。
- 法术抗性加算并在伤害计算时限制到 `0..100`：覆盖
  `1165/1166/1170 +70`、`1230 +60`。
- 攻速加算按
  `ceil(BaseIntervalTicks × 100 / (100 + AttackSpeedAdditive))`
  重算攻击间隔：覆盖 `10127 +100` 的底层语义。
- 物理/法术最终承伤千分比按稳定能力 ID 顺序相乘，真实伤害不受影响：覆盖
  `10127` 的物理/法术承伤 `50%`。

### 持续生命与寿命

- 每秒生命变化使用整数余量累计，不丢失不能整除 20 TPS 的部分。
- 正数在每 Tick 治疗并限制到 MaxHP；负数作为无来源真实生命流失，但不会播放 Hit。
- 覆盖 `1043 +160/s`、`1044/1061 +400/s`、
  `1062 -330/s`、`1089 -800/s` 的底层语义。
- 固定寿命在 `ActivationTick + LifetimeTicks` 精确归零并进入普通死亡清理：
  覆盖 `10002 6s=120 Tick` 与 `1146 30s=600 Tick` 的底层语义。
- 新增 `HealthChanged` Core 事件，Presentation Track 和事件播放均只更新血条，不伪造受击动画。

### 受击反伤

- 受敌方单位伤害时，对该次攻击来源造成固定无来源伤害；反伤不递归触发另一轮反伤。
- 反伤与原受击同 Tick 结算，随后统一处理死亡，因此致死一击仍完成已经由该次受击触发的反伤。
- 支持伤害类型、目标防御/法抗与目标承伤修正；覆盖
  `7002/10126` 的 `200` 无来源法术反伤底层语义。
- 查证依据：
  [PRTS 矿脉守卫](https://prts.wiki/w/%E7%9F%BF%E8%84%89%E5%AE%88%E5%8D%AB) 与
  [PRTS 异质源石虫](https://prts.wiki/w/%E5%BC%82%E8%B4%A8%E6%BA%90%E7%9F%B3%E8%99%AB)
  均明确描述“受到单位伤害时，对伤害来源造成 200 无来源法术伤害（无视其可选性）”。

聚焦验证：

- `BondsPassiveCombatModifierEditModeTests` 中本节对应的 `8` 项验证通过，覆盖三阻挡、法抗、攻速、物理/法术减伤且不影响真实伤害、每秒 `330` 的余量累计、每秒 `400` 治疗、120 Tick 寿命、致死受击反伤，以及 `HealthChanged` Track 采样。

### 生命阈值战斗修正

- 支持 `<=` 与 `<` 两种生命千分比阈值；比较使用整数交叉相乘，不引入浮点分歧。
- 支持“只要在阈值内就生效”和“首次越过阈值永久/限时生效”两种触发方式。
- 生效项可组合攻击倍率、防御倍率、阻挡数加算、攻速加算和移速倍率；多个能力按稳定 ability ID 顺序应用。
- 阈值状态在每 Tick 开始、直接伤害结算后和持续生命变化后刷新。同 Tick 同批伤害使用伤害前状态，下一阶段才切换阈值，避免单位 ID 顺序改变结算结果。
- 持续型阻挡加成因治疗失效时，会按 unit ID 稳定保留容量内关系并立即解除多余阻挡。
- 框架可表达 `1025` 的 `<=50%` 攻击倍率、`1232` 的 `<50%` 防御 `4×` 与阻挡 `+1`、`1264` 的首次受伤后 300 Tick 攻速/移速提升，以及 `1274` 的首次 `<50%` 后永久攻速/移速提升。
- `1025` 精英等级对应 `2×/2.8×` 的数据选择仍需下一代运行时目录接入；当前 Core 只负责执行已选定的倍率。

聚焦验证：

- `BondsPassiveCombatModifierEditModeTests`：合计 `12/12` 通过，其中新增 `4` 项覆盖持续攻击倍率、持续防御与阻挡、首次触发限时攻速与移速、治疗越过阈值后的确定性阻挡回收。

### 未被阻挡时减伤

- 每次伤害实际结算时读取目标的对称阻挡关系；当前没有阻挡关系时才应用条件修正。
- 物理与法术承伤千分比独立配置，真实伤害不受影响；与常驻承伤修正按稳定 ability ID 顺序执行。
- 覆盖 `1262` 未被阻挡时物理/法术承伤 `50%` 的完整 Core 语义。

聚焦验证：

- `BondsPassiveCombatModifierEditModeTests` 新增 `1` 项四场景断言，当前合计 `13/13` 通过：未阻挡的物理/法术伤害为 `500/500`，真实伤害仍为 `1000`，建立阻挡后物理伤害恢复为 `1000`。

### 攻击序列倍率

- 攻击计数在单位确实找到合法目标、处于范围内并成功开始 Attack 时递增；仅索敌、移动或冷却等待不计数。
- 本次攻击倍率在 Attack 开始时与当前阈值攻击力一起封存，目标随后死亡导致出伤取消也不会回滚已经开始的攻击次数。
- 支持“仅指定序号一次”和“从指定序号起按固定间隔重复”；多个序列倍率按稳定 ability ID 顺序相乘。
- 覆盖 `1314` 第一次攻击 `200%`，以及 `1371` 第 `3/6/9...` 次攻击 `130%` 的完整 Core 语义。

聚焦验证：

- `BondsPassiveCombatModifierEditModeTests` 新增 `2` 项，当前合计 `15/15` 通过：一次性序列为 `200/100/100`，每三击循环序列为 `100/100/130/100/100/130`。

### 指定攻击前的永久状态切换

- 初始状态与解放状态由已开始攻击次数确定；前三次攻击保持初始属性，第 4 次攻击开始前原子切换，第四击立即使用解放后的属性。
- 初始状态支持攻速、固定防御加成；解放状态支持攻击倍率、法抗、每秒回复以及目标防御倍率。破防倍率与本次攻击力在 Attack 开始时一同封存，目标后续属性变化仍由伤害 Tick 读取。
- 可表达 `1116` 的禁锢攻速 `-50` 与解放攻击 `+50%`、`1118` 再附加无视 `60%` 防御、`1119` 再附加法抗 `+40` 与每秒回复 `300`，以及 `1121` 自身禁锢防御 `+300`/解放攻击 `+50%` 的 Core 数值语义。
- 尚未接入 A/B 状态动画；`1121` 第一次解放时同步解放友方单位也尚未实现，因此 `1121` 仍为部分完成。

聚焦验证：

- `BondsPassiveCombatModifierEditModeTests` 新增 `2` 项，当前合计 `17/17`：验证攻击 Tick 为 `1/5/9/13/15`、伤害为 `100/100/100/150/150`，并覆盖第 4 次攻击时防御 `300→0`、法抗 `0→40`、攻击 `200→300`、目标防御按 `40%` 结算及解放后每 Tick 回复 `15`。

### 死亡后继与分裂

- Death 时封存后继配置；支持立即或延迟 Spawn、固定数量、方形稳定随机位置、加权稳定随机类型和后继实例移速倍率。
- 同阵营只剩待生成后继时不会提前结算失败。延迟到期顺序固定为 due Tick、死亡单位 ID、ability ID；候选选择、位置和动态负 ID 可重复运行。
- Spawn 事件在死亡 Tick 或指定延迟 Tick 产生，后继沿用既有规则在下一 Tick 激活。
- 可表达 `1131` 延迟 `0.2s=4 Tick` 后在 40cm 区域生成 2 个后继、`1132` 同条件生成 3 个、`1281` 死亡 Tick 在中心生成 3 个 `1277`，以及 `1437` 的 `40%/60%` 类型选择与后继移速 `+200%`。
- 当前 Core 没有障碍/地形通行图；“最近可通行地块”暂按 9×8 战场中最近的非门格处理，等主分支提供地形权威源后需替换候选集合。
- `1131/1132` 的精二后继类型应由精英化单位目录选择对应 ability；当前 Core 不自行读取或猜测精英化数据。

聚焦验证：

- `BondsPassiveCombatModifierEditModeTests` 新增 `3` 项，当前合计 `20/20`：覆盖延迟 4 Tick 且不提前判胜、死亡 Tick 中心生成 3 个后继、加权选择跨两次运行一致，以及 `3×` 后继移速同时进入 Runtime 与 SpawnSnapshot。
- `BattleCoreEditModeTests`：终局与动态生成回归 `57/57` 通过。

### 数值光环

- 支持全场或整数厘米半径、友方/敌方、是否排除来源、以及同 ability ID 不叠加；来源按 unit ID、ability ID 稳定决胜。
- 光环在每 Tick 开始、移动后和死亡清理后刷新。移动使用 Tick 开始时的范围关系，攻击/出伤使用移动后的关系，死亡来源不会在同 Tick 后续生命回复阶段继续提供效果。
- 数值项包括攻击倍率、防御加算、法抗加算、攻速倍率、移速倍率和每秒生命变化。
- 可表达 `1017` 半径 250cm 的其他友方防御 `+300`、`1042` 半径 250cm 敌方攻速乘 `50%`、`1355` 半径 250cm 其他友方法抗 `+30`，以及 `1080` 全场友方攻击乘 `110%`/防御 `+100` 的直接数值部分。
- `1017/1042/1355` 的不可选中、无攻击、零阻挡与直冲敌方门由特殊索敌框架表达，但真实单位仍等待下一代目录接线。
- “可被沉默”尚无 Silence 运行时状态；`1080` 的 `<战术命令>` 标签，以及 `1078/1083` 对该标签的条件增益尚未实现，因此 `1080` 仍为部分完成。

聚焦验证：

- `BondsPassiveCombatModifierEditModeTests` 新增 `2` 项，当前合计 `22/22`：覆盖两个同名来源只生效一次、排除来源、半径内外、防御/法抗，以及全场友方攻击/防御与近距离敌方攻速 `20→40 Tick`。

### 阻挡对手与同类型邻近

- 阻挡评估后立即刷新关系型外部修正；阻挡关系解除或来源死亡后，同 Tick 后续攻击不再保留旧减速。
- `1138` 的阻挡对手攻速直接乘 `20%`，按 ability ID 不叠加；两个同名来源同时与一个二阻挡单位建立关系时仍只生效一次。
- `1169` 可由常驻法抗 `+70` 与同类型邻近效果组合：150cm 内每个同阵营同 type ID 实体使自身防御 `+200`，逐个叠加。
- 当前 PvP Core 的阻挡关系是对称关系，不保存“谁主动阻挡谁”；`1138` 因此对关系中的敌方对手生效。若后续规则引入单向阻挡语义，需要扩展关系模型。

聚焦验证：

- `BondsPassiveCombatModifierEditModeTests` 新增 `2` 项，当前合计 `24/24`：覆盖两个 `1138` 来源不叠加（攻击间隔 `10→50 Tick`），以及三个方阵单位中间获得两层、两端各一层防御且法抗保持 `+70`。

### 确定性物理/法术闪避

- 待结算攻击额外封存攻击序号；物理/法术攻击到达伤害 Tick 时，以 battle ID、攻击者/目标 unit ID、ability ID、伤害 Tick 和攻击序号生成稳定千分位判定。相同输入的回放与独立 Core 运行得到相同闪避序列，不使用进程随机数。
- 多个闪避能力按 ability ID 稳定顺序分别判定，任一成功即令该次攻击伤害为 `0`；`Damage` 事件仍保留，供后续 Presentation 接入 Miss 表现。真实伤害不参与闪避。
- 覆盖 `1231` 的物理/法术闪避 `80%` Core 数值语义。PRTS 也记录为 80%，且处于部分异常状态时失效；当前 Core 尚无晕眩、冻结、沉睡、浮空、束缚、无法行动和沉默状态，因此本批不伪造异常失效。
- PRTS 没有说明无来源伤害的闪避边界；当前仅对正常 `PendingAttack` 判定，受击反伤与持续生命变化保持不变，并把该边界留待状态/通用伤害上下文落地后统一决定。

聚焦验证：

- `BondsPassiveCombatModifierEditModeTests` 新增 `2` 项，当前合计 `26/26`：覆盖物理/法术 `100%` 边界、真实伤害不闪避、`80%` 输入进入稳定摘要，以及两次独立运行的伤害序列完全一致且同时出现闪避与命中。
- 查证参考：[PRTS 深溟猎食者](https://prts.wiki/w/%E6%B7%B1%E6%BA%9F%E7%8C%8E%E9%A3%9F%E8%80%85)。

### 延迟死亡范围伤害

- 单位死亡时封存阵营、位置、当时有效攻击力、ability ID 和爆炸配置；到期项按 due Tick、死亡单位 ID、ability ID 稳定结算，爆炸中心不会跟随已经死亡的 Runtime 实体变化。
- 支持物理/法术/真实伤害类型、攻击力千分比、整数厘米半径与正数延迟 Tick。同 Tick 多个爆炸先按目标聚合再扣血，避免爆炸来源或 unit ID 遍历顺序改变存活结果。
- 只剩待爆炸效果的阵营仍保持战斗待定，爆炸结算后才进入终局判断；范围伤害读取目标当 Tick 防御、法抗和承伤修正，但不作为普通攻击触发受击反伤或闪避。事件保留已经死亡的 owner unit ID 以供审计与 Presentation 定位，不把它恢复成存活伤害来源。
- 可表达 `1021` 的 `20 Tick` 延迟、125cm 半径、攻击 `400%` 物理爆炸，以及 `1089` 与既有每秒自损组合后的 `26 Tick` 延迟、150cm 半径、攻击 `200%` 法术爆炸。
- 当前单位数据没有独立的空中/地面命中标签，故两项“不可对空”尚未接入；本批不以 `actionMethod` 猜测飞行状态。

聚焦验证：

- `BondsPassiveCombatModifierEditModeTests` 新增 `1` 项，当前合计 `27/27` 通过：覆盖 `26 Tick` 延迟、爆炸前不提前判胜、150cm 内两个目标各受 `200` 法术伤害、范围外目标不受伤、输入摘要与 Presentation Track 编译。

### 攻击范围伤害

- 攻击开始时已封存的攻击序号决定本次是否转为范围攻击；支持从指定序号开始一次性或按固定间隔重复。范围攻击沿用已封存的本次攻击力和破防倍率，再应用独立的伤害类型与攻击力千分比。
- 圆形模式以主目标在伤害 Tick 的位置为中心，按整数厘米平方距离选取所有敌方存活实体；十字模式把主目标与候选当前位置映射至最近的 9×8 战场格（距离、X、Y 稳定决胜），覆盖中心格及上下左右一格。
- 主目标只结算一次；范围中的每个目标各自产生 `Damage` 事件，并分别执行普通攻击的目标防御/法抗、承伤、闪避与受击反伤语义。次要目标不参与主攻击的索敌资格判断。
- 覆盖 `10078` 每次攻击对目标150cm内所有敌方造成攻击 `100%` 物理伤害，以及 `1372` 第 `3/6/9...` 次攻击对目标中心十字五格造成攻击 `100%` 物理伤害。
- BONDS 尚未给 `10078` 增加不可对空限制；`1372` 也没有额外写目标可选性条件。本批按“所有敌方单位”处理次要目标，并等待未来统一的空中/地面与范围伤害选取规则。

聚焦验证：

- `BondsPassiveCombatModifierEditModeTests` 新增 `2` 项，当前合计 `29/29` 通过：圆形溅射命中中心与100cm邻近目标而不命中200cm目标；十字溅射前两击只命中主目标，第三击命中主目标与正交邻格且不命中对角格。

### 命中后持续生命流失

- 只有普通攻击实际造成正伤害时才施加效果；闪避或其他零伤害结果不施加。范围攻击的每个实际命中目标分别进入同一流程。
- Runtime 按 ability ID 保存目标身上的持续伤害状态；同名效果再次命中时更新每秒伤害并把失效 Tick 重置为“当前伤害 Tick + 持续 Tick”，不新增叠加层。不同 ability ID 可并存。
- 持续伤害与被动生命变化共用20 TPS整数余量结算，并输出 `HealthChanged`/无来源真实生命流失；它不播放普通 Hit，也不递归触发受击反伤或闪避。施加所在伤害 Tick 即计入第一 Tick，失效 Tick 在生命周期阶段前移除，因此100 Tick持续时间精确结算100个 Tick。
- 覆盖 `1335` 命中后施加 `100 Tick`、`20/s` 的【流血】，同名重复只刷新持续时间。

聚焦验证：

- `BondsPassiveCombatModifierEditModeTests` 新增 `1` 项，当前合计 `30/30` 通过：两个同名来源同 Tick 命中不会叠为 `40/s`，第82 Tick再次命中把效果刷新到测试结束；149个活跃 Tick 合计流失 `149`，四次直接伤害合计 `40`。

## 后续能力批次

以下仅表示实现顺序，不表示已完成：

1. 攻击计数剩余：`1121` 群体解放、`1322/1372/2031/2033`。
2. 光环与邻近效果剩余：`1080` 标签传播、`1146` 定向移动与攻击范围光环。
3. 其他召唤、碰撞与传送：`10006/10038/10039/10077/1095/1502`。

## 已跳过或尚不能决断

- `10039` 缺少蓄力时长、伤害倍率和范围，不能实现数值效果。
- `10001` 还需要“切换为不可阻挡状态”和 `Skill_Begin` 状态动画；现有正向属性阈值修正不能代替状态切换。
- `10004` 要在 `Skill_A` 切换动画完整播放后才回满生命，当前通用阈值修正没有延迟治疗效果。
- `10006` 的永久移速倍率已有 Core 表达，但四个非门相邻格召唤尚未实现，不能标记整项完成。
- `1502` 最新 BONDS 已确认“仅被阻挡时向敌方门瞬移 1.5 格，初始/需求 SP 为 15/15”；但该单位尚未进入 v2 权威源，技能动画 key/源时长也没有可用数据，因此本批不伪造接线。
- 冲家事件如何改变单场胜负及何时结算玩家生命，当前 Core 没有确认闭环；现阶段只实现移动，不伪造胜负。
- BONDS 中绝大多数单位尚未进入 v2 权威源；在新运行时目录格式落地前，只能实现可独立验证的 Core 机制，不能宣称生产接线完成。

## 当前未验证项

- 未运行 PlayMode 的真实 Spine Skill 播放冒烟；当前只验证了 EditMode Track/Event 数据流和编译。
- 未运行 Windows Player 构建；本批次没有场景、Prefab、Package 或平台配置修改。
- Unity 测试在结构化 XML 写入完成后受到 Unity China 配置请求残留影响未自行退出；测试 XML 全部通过，进程在 10 秒收尾上限后被终止。
